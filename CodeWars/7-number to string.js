function numberToString(num) {
  return `${num}`; //return ("" + num);
}

// Method toString pada JavaScript
// method ini tidak dapat mengembalikan nilai null dan undefined
// const numberToString = (num) => num.toString();

//dapat digunakan ketika nilai atau valuenya null atau undefined
// function numberToString(num) {
//   return String(num);
// }

console.log(numberToString(67));
